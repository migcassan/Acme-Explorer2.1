export class InfoMessage {

    code: string;
    cssClass: string;
    timeout: 18000;

    constructor (code: string, cssClass: string) {
        this.code = code;
        this.cssClass = cssClass;
        this.timeout = this.timeout;
    }
}